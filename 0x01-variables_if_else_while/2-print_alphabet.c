#include <stdio.h>
/**
 * main - print different digit
 * Return: Always 0
 */
int main(void)
{
	int data1, data2;

	for (data1 = 0; data1 < 9; data++)
	{
		putchar((data1 % 10) + '0');
		putchar((data2 % 10) + '0');
			continue;
		putchar(',');
		putchar(' ');
	}
}
		putchar('\n');
		return (0);
}
